import { StockChartDirective } from './stock-chart.directive';

describe('StockChartDirective', () => {
  it('should create an instance', () => {
    const directive = new StockChartDirective();
    expect(directive).toBeTruthy();
  });
});
